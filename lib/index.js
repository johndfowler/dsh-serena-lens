// dsh-serena-lens — node half.
// Intentionally a no-op: the lens is client-side only (browser half mounts
// the floating button + iframe panel for the m4/m5 Serena dashboards).
export default function dshSerenaLens() {}
